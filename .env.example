# Local Postgres (docker compose up -d postgres)
DATABASE_URL=postgres://tripwire:tripwire@localhost:5432/tripwire

# GitHub App (step 3) — register per VERIFICATION-QUEUE.md #1
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_WEBHOOK_SECRET=

# Better Auth (step 8) — GitHub OAuth app per VERIFICATION-QUEUE.md
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=

# Review agent (step 9)
ANTHROPIC_API_KEY=
AI_REVIEW_MODEL=claude-fable-5
